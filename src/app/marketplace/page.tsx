'use server';

import Marketplace from './components/Marketplace';

const MarketplacePage = async () => {
  // Here you can call API endpoints, fetch NFTs, etc.
  // For now, we’ll use placeholder data inside MarketplaceClient

  return <Marketplace />;
};

export default MarketplacePage;
